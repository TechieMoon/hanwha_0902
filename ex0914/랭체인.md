# 랭체인 사용법

다음과 같이 임포트 해야함

```python
from langchain_openai import ChatOpenAI
```

그 전에 패키지 설치해야 함

```cmd
pip install langchain_openai
```

다음과 같이 코드를 작성하면 ChatGPT의 답변을 얻을 수 있다.

```python 
llm = ChatOpenAI(
    temperature=0.1,
    model="gpt-4o-mini"
)

question = "미국의 수도는 어딜까요?"

response = llm.invoke(question)

print(response.content)
```

# 랭스미스 사용법

```python
from langchain_openai import ChatOpenAI 

llm = ChatOpenAI(model="gpt-4o-mini")
question = "일본의 수도는 어디인가요?"
response = llm.invoke(question)
print(response.content)
```

랭체인이랑 코드는 같지만 다음과 같은 이미지처럼 langsmith 사이트에 기록이 저장된다.


![langsmith_tracing](https://github.com/TechieMoon/hanwha_0902/blob/main/ex0914/langsmith.png)